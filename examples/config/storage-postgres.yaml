storage:
  backend: postgres
  postgres:
    connection_url: "postgresql://agentic_identity_broker:password@localhost:5432/agentic_identity_broker?sslmode=require"
  timeouts:
    read: 5s
    write: 10s
