/**
 * Button component - Re-export from design system
 *
 * This maintains backward compatibility while using the design system implementation.
 * For new code, import directly from the design system:
 * import { Button } from '@design-system/components/primitives/Button';
 */

export {
  Button,
  type ButtonProps,
  Button as default,
} from '@design-system/components/primitives/Button';
